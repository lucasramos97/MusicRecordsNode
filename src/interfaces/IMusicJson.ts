/* eslint-disable camelcase */
export interface IMusicJson {
  id?: number,
  title: string,
  artist: string,
  release_date: string,
  duration: string,
  number_views?: number,
  feat?: boolean,
  userId?: number,
  created_at?: string,
  updated_at?: string,
}
